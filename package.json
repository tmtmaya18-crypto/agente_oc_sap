{
  "name": "agente-oc-sap",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Agente conversacional que prepara, valida y crea órdenes de compra en un SAP simulado (reto técnico 03).",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "demo": "bun run demo.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "modulo": "bun run scripts/empaquetar-modulo.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "zod": "^4"
  },
  "devDependencies": {
    "bun-types": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
